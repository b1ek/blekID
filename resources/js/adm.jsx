import ReactDOM from 'react-dom/client';
import React from 'react';
import Panel from './admin/panel.jsx';

const root = ReactDOM.createRoot(document.getElementById('adm_panel'));
root.render(
    <div>
        <Panel />
    </div>
)
